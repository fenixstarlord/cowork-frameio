{
  "mcpServers": {
    "frameio": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--directory", "mcp-server", "python", "-m", "frameio_mcp.server"],
      "env": {
        "FRAMEIO_CLIENT_ID": "",
        "FRAMEIO_CLIENT_SECRET": ""
      }
    }
  }
}
